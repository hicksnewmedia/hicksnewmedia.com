# ════════════════════════════════════════════════════════════════════════
# Netlify configuration for hicksnewmedia.com
# https://docs.netlify.com/configure-builds/file-based-configuration/
#
# Subdomains served from one Netlify site:
#   hicksnewmedia.com           → /index.html          (main site)
#   mediakit.hicksnewmedia.com  → /mediakit/index.html (browser media kit)
#   links.hicksnewmedia.com     → 301 → shelf.hicksnewmedia.com (retired Aug 2026)
#
# Subdomain pages reference shared assets at /assets/*, which serve
# from the same site root for every subdomain.
#
# Also serves:
#   /ask  → Ask James AI chatbot (dedicated page)
#   /book → Cal.com booking page (inline calendar embed)
#   /.netlify/functions/ask-james → chatbot serverless function
# ════════════════════════════════════════════════════════════════════════

[build]
  publish = "."
  # Functions live in netlify/functions/ for the Ask James chatbot
  functions = "netlify/functions"

[functions]
  # Faster bundling for the ask-james function
  node_bundler = "esbuild"

# ────────── Subdomain routing ──────────
# Only rewrite the root path "/" for each subdomain. All other paths
# (like /assets/foo.png) serve naturally, so the shared /assets/
# folder works across every subdomain without duplication.

[[redirects]]
  from = "/"
  to = "/mediakit/index.html"
  status = 200
  force = true
  conditions = {Host = ["mediakit.hicksnewmedia.com"]}

# links.hicksnewmedia.com → shelf.hicksnewmedia.com (301 permanent)
# The Stack was folded into The Shelf in Aug 2026. Every path on the old
# subdomain lands on The Shelf so legacy bookmarks and bio links survive.
# NOTE: keep links.hicksnewmedia.com as a domain alias on this site —
# removing the alias is what breaks host-conditional rules.
[[redirects]]
  from = "/*"
  to = "https://shelf.hicksnewmedia.com"
  status = 301
  force = true
  conditions = {Host = ["links.hicksnewmedia.com"]}

# Pretty URL for /ask → /ask.html (Ask James chatbot dedicated page)
[[redirects]]
  from = "/ask"
  to = "/ask.html"
  status = 200

# Pretty URL for /book → /book.html (Cal.com booking page)
[[redirects]]
  from = "/book"
  to = "/book.html"
  status = 200

# ────────── Security headers (apply to all subdomains) ──────────
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options          = "SAMEORIGIN"
    X-Content-Type-Options   = "nosniff"
    Referrer-Policy          = "strict-origin-when-cross-origin"
    Permissions-Policy       = "geolocation=(), microphone=(), camera=()"

# ────────── Caching ──────────
# Long cache for static assets (logos, favicons, PDF) — stable URLs
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Short cache for HTML so updates show up quickly
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Never cache the chatbot function responses
[[headers]]
  for = "/.netlify/functions/*"
  [headers.values]
    Cache-Control = "no-store, max-age=0"
